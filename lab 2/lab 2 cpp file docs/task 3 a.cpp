#include <iostream>   // it tells the computer we want to read input from the user and print output to the screen
using namespace std; // this lets us write cout and cin without extra std::

int main()// this is where the program starts running
{
    int a = 25;        // changing value from 10 to 20 so the compiler will show whatever the value of a is i.e 20 instead of 10
    cout << a;         // prints 25

    return 0;      // program finished successfully    
}