// Include input and output library
#include <iostream>
using namespace std;

int main()
{
    // Loop runs two times
    for (int i = 0; i < 2; i++)
    {
        // Print the value of i
        cout << i << "\n";// print 0 and 1 on the screen b/c the condition is to hoew the number less than 2 so 0 and 1 is less than 2
    }

    // End the program
    return 0;
}