#include <iostream>   
using namespace std; // use the namespace std to avoid std :: before every cout

int main() // the execution of program starts from here
{
    float a = 3;    // 'a' assinig integer to float can show integer but integer cant show decimal number
    cout << a;         // prints 3 b/c float (the float data type usrd to print decimal number as well as whole number  in the function )

    return 0;       // program finished successfully   
}